import Vuex from 'vuex'

const createStore = () => {
  return new Vuex.Store({
    state: {
      //
    },
    mutations: {
      //
    },
    actions: {
      // async nuxtServerInit({commit}, {app}) {
      // await app.$axios.$get('/site_info').then(res => commit('',res))
      // }
    }
  })
}

export default createStore